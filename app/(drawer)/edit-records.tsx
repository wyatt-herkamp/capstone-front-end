import { View, Text } from 'react-native';

export default function Records() {
  return (
    <View className="flex h-screen items-center justify-center">
      <Text>Add functionality to search records by ID or name</Text>
      <Text>Add functionality to Add a new record (same as adding a new patient I guess?)</Text>
    </View>
  );
}
